מעודכן לתאריך ה-19.04.2023:
- להוסיף ב-API של ה-OSA וב-GUI את ה-Resuolation Parameter.
- לשנות ב-GUI את פונקציית Beer-Lambert ו-Allan Variance ל-Spectrum Analyzer.
הפרמטרים שזה יקבל הם:
אורך המוליך גל במ"מ (mm), להוסיף אופציה להכנסה של נקודת בליעה, או Automate, להוסיף את זמן המדידה הכולל ואת האינטרוול.
- להוסיף חלון גלילה של נתונים על החומר, עם אופציה להוסיף לאותה תיקייה Database של נתונים.
- להציג את גרף Beer-Lambert (ריכוז החומר לפי זמנים).
- לסדר עדכון טקסט - graphStatusText.

חשוב:
היה באג שאני ועוזיאל הגדרנו CF=1650nm, Span=40nm ובגרף שהודפס יצא תתוצאה של 1475nm עד ל-1525nm.


מה סודר:
- להוסיף ב-API של ה-OSA וב-GUI את ה-Sensetivity Parameter.
- קינפוג והפעלה של הלייזר - שיהיה רציף מבלי לכבות את הלייזר בכל פעם לפני הקינפוג.
- שם הקבצים, להוסיף את הפרמטרים של המדידה בשם התיקייה.


- לסדר את עניין הטרדים.
- לסדר את עניין שה-GUI הראשי יגיב.
לסדר הכל ב-while אחד.
- לסדר את הפלוט (להוסיף toolbar ושיפעל בלי ה-debug mode).
- שאחרי סריקה האופציה של התוצאה החדשה תופיע.
- כפתור Delete למחיקה.
- להוסיף סמן.
- לסדר הדפס של ציר X.